[project]
name = "Workflow Organising Tool"
version = "0.2"
description = "A better way of organising my workflow."
readme = "README.adoc"
requires-python = ">= 3.14"
dependencies = [
  "toga>=0.5.6",
]