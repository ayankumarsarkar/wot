[project]
name = "workflow_organising_tool"
version = "0.2"
description = "A better way of organising my workflow."
readme = { file = "README.adoc", content-type = "text/markdown" }
requires-python = ">= 3.14"
dependencies = [
  "toga>=0.5.6",
]

